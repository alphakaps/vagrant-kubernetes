
k delete ns authentik && k create ns authentik && k delete pv pv-static && kaf pv.yml

k delete ns keycloak && k create ns keycloak && k delete pv pv-static && kaf pv.yml

helm upgrade --install keycloak bitnami/keycloak --version 24.4.9 -n keycloak -f values.yml

helm upgrade --install authentik authentik/authentik -n authentik -f authentik-values.yml

kubectl apply -f https://k8s.io/examples/admin/dns/dnsutils.yaml

kubectl exec -i -t dnsutils -- bash

kubectl run -it --rm --restart=Never --image=busybox:1.28.4 dns-test -- sh

kubectl delete pods -n kube-system -l k8s-app=kube-dns

kubectl exec -it deployment/authentik-server -n authentik -c server -- ak changepassword akadmin