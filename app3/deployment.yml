apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3
  namespace: app3
  labels:
    app: app3
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app3
  template:
    metadata:
      labels:
        app: app3
    spec:
      serviceAccountName: app3
      containers:
      - name: app3
        image: alphakaps/helloworld
        ports:
        - containerPort: 8080
