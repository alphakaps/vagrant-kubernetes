apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app3-httproute
  namespace: app3
spec:
  parentRefs:
  - name: traefik-gateway
    namespace: traefik
  hostnames:
  - "app3.traefik.lc"
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
      - name: app3-service
        port: 8080
        namespace: app3
