apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: helloworld-httproute
  namespace: helloworld
spec:
  parentRefs:
  - name: traefik-gateway
    namespace: traefik
  hostnames:
  - "www.helloworld.com"
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
      - name: helloworld-service
        port: 8080
        namespace: helloworld