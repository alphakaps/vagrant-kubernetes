apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: app-httproute
  namespace: app
spec:
  parentRefs:
  - name: traefik-gateway
    namespace: traefik
  hostnames:
  - "app.traefik.lc"
  rules:
    - filters:
      - type: ExtensionRef
        extensionRef:
          group: traefik.io
          kind: Middleware
          name: authentik-forwardauth
      matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
      - name: app-service
        port: 8080
        namespace: app
